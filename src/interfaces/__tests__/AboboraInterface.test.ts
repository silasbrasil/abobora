import { expect } from "chai";
import "mocha";

describe("Abobora interface", () => {
  it("should return false", () => {
    const expectation: boolean = false;
    expect(expectation).to.equal(false);
  });
});
